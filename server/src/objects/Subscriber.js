class Subscriber {

    constructor(id, camObj, cubeObj, uObj) {
        this.id = id;
        this.camID = camObj;
        this.cubeID = cubeObj;
        this.uID = uObj;
    }

}

module.exports = Subscriber;
